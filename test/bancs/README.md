# Bancs du pont vocal (Grok Voice au téléphone)

Outils de mesure écrits pendant le chantier « fins de phrase coupées et latence » de Palazzo (16 et 17/09/2026). Ils parlent à la vraie API de Grok : la clé `XAI_API_KEY` (et `OPENAI_API_KEY` pour les transcriptions) est lue dans le vault (`VAULT`, par défaut `C:/Users/msi/.secrets/api-keys.env`) et jamais affichée. Sorties dans `test/bancs/.sorties` (ignoré par git), ou `BANC_SORTIE`.

La session jouée est `fixtures/session-palazzo.json` (ce que Dale Voz servait au pont le 17/09) ; `BANC_SESSION=<fichier>` en joue une autre. `fixtures/client-polly.wav` est la voie client d'un appel de test (voix de synthèse Twilio : question 15,22 à 18,40 s, « Mmm » 22,46 s, « Attendez… sans gluten ? » 25,22 à 29,02 s, « Rucola » 41,12 à 43,12 s).

| Banc | Ce qu'il tranche | Commande | Résultat du 16-17/09 |
|---|---|---|---|
| `banc-appel-local.mjs` | Le pont ENTIER sur ce poste, un faux Twilio joue un client qui réagit à l'agent (question, « mmm », interruption, commande). Enregistrement stéréo et journal, dialogue en fin de journal. `SCENARIO=anticipation` : phrase avec une pause de 460 ms dite quand l'agent se tait, puis un bruit sans mot, puis une question. `ANTICIPATION_MS=0` pour comparer, `MMM_APRES_MS=700` pour forcer le « Mmm » d'attente et l'entendre dans l'enregistrement. `HEDGE_APRES_MS=300` fait demander la doublure à CHAQUE tour, `AMBIANCE_APRES_MS=600` remplit tous les blancs. ⚠ La sortie console est FILTRÉE : le journal entier est dans `.sorties/<nom>.log`, et le filtre efface aussi ` sid=…`, ce qui fait croire à des lignes tronquées. | `node test/bancs/banc-appel-local.mjs pont` (ou `grok` pour l'ancien mode) | pont : premier son 1,47 s, « mmm » ignoré, réponse jouée en entier ; grok : 2,37 s, réponse tronquée. 17/09, attente de fin de phrase 900 ms : sans anticipation 1,86 à 1,90 s sur les tours où Grok est rapide, avec 1,30 à 1,43 s ; pause de la phrase : anticipation annulée en 0,2 s, une seule ligne client ; bruit : demande ignorée relâchée en 1,01 s |
| `banc-anticipation-protocole.mjs` | Ce que l'anticipation demande à l'API : `response.cancel` (avant et après `response.created`, sans réponse active), `conversation.item.delete`, et une demande de réponse sur un son sans mot. | `node test/bancs/banc-anticipation-protocole.mjs [1\|2\|4]` | annulation : `response.done` au statut `cancelled`, y compris envoyée avant la création ; annulation à vide sans effet ni erreur ; `metadata` de `response.create` jamais renvoyé ; « mmm » de 0,8 s ou silence validés seuls : AUCUNE réponse, aucune erreur, en 20 s |
| `banc-anticipation-ids.mjs` | Au rythme réel du pont, ce que le MODÈLE reçoit d'une phrase validée trop tôt puis annulée, selon trois façons de reprendre. | `node test/bancs/banc-anticipation-ids.mjs [A\|B\|C]` | le message du client garde le même identifiant après le commit et la suite s'y ajoute ; A (l'effacer, renvoyer toute la phrase) : le modèle reçoit le début en double ; B (le garder, n'effacer que la réponse, envoyer la suite) : phrase entière, une fois ; C (effacer, vider le tampon, attendre, renvoyer) : propre aussi. Le pont fait B |
| `banc-latence-grok.mjs` | Premier son d'une réponse, avec et sans la carte dans le contexte, en alternance. | `node test/bancs/banc-latence-grok.mjs 5` | médiane ~0,8 à 0,9 s ; pics à ~3,7 s une réponse sur quatre, sans lien avec la taille du prompt |
| `banc-vad-grok.mjs` | Retard du détecteur de fin de phrase de Grok selon seuil et silence. | `node test/bancs/banc-vad-grok.mjs test/bancs/fixtures/client-polly.wav 14.0 20.0 15.22 18.40 2` | fin annoncée 1,4 à 1,6 s après le dernier mot, quels que soient les réglages |
| `banc-manuel-tampon.mjs` | Grok arrête-t-il la réponse qu'il génère quand il entend le client ? | `node test/bancs/banc-manuel-tampon.mjs` | témoin 23,8 s d'audio ; « mmm » en direct 1,2 s ; « mmm » retenu puis envoyé 23,5 s |
| `banc-fin-question.mjs` | Fin abrupte d'une réponse selon sa tournure (question, question + « Je vous écoute ! », points de suspension, style indirect). | `node test/bancs/banc-fin-question.mjs` | question 5/5 coupées (27/27 sur toutes les voix) ; question + « Je vous écoute ! » 0/5 |
| `banc-parades-pics.mjs` | Les trois parades aux pics de Grok, une fois écartés le modèle, le format audio, la vitesse et la relance. `effort` : un `response.create` qui porte un objet `response` repart-il en réflexion « high » ? `rang` : l'historique qui grossit fait-il monter la latence ? `hedge` : les pics sont-ils indépendants d'une session à l'autre ? `injection` : peut-on poser dans une session une réponse qu'elle n'a pas produite ? | `node test/bancs/banc-parades-pics.mjs [effort\|rang\|hedge\|injection\|tout] [N]` | **effort : non**, les trois branches sont indistinguables et l'ordre s'inverse d'un run à l'autre. **rang : non**, 30 tours d'affilée 11 % de pics contre 12 % en sessions de 5 tours, aucune montée avec le rang. **hedge : oui**, 24 tours, A 0 pic (max 1596 ms), B 2 pics (max 2418 ms), la première des deux 0 pic, max 1007 ms, p90 986 ; jamais les deux lentes. **injection : oui**, `role: "assistant"` accepté (accusé `conversation.item.added`) et relu fidèlement |
| `banc-parades-pics.mjs avide` | Que fait Grok quand on lui demande une réponse SANS nouvelle entrée du client ? C'est ce qui arrive dès que le pont valide un tour sur un bruit sans mot (« mmm », un souffle), qui ne crée aucun message. | `node test/bancs/banc-parades-pics.mjs avide` | **il REDIT sa dernière réponse, mot pour mot.** Première demande à vide : ignorée en silence, aucun son. Seconde : « Non, on n'a pas de pizzas sans gluten. », identique au tour précédent. C'est la cause des répétitions entendues sur la ligne |
| `banc-parades-pics.mjs reprise` | Une réponse annulée puis remplacée par celle de la doublure laisse-t-elle un élément qui fait répéter le modèle ? Avec et sans effacement des éléments annulés. | `node test/bancs/banc-parades-pics.mjs reprise` | non, les deux enchaînent correctement : l'élément annulé n'y est pour rien, inutile de l'effacer |
| `banc-transport-binaire.mjs` | `audio.*.transport: "binary"` (doc xAI) supprime le base64 des trames WebSocket : est-ce que ça se voit sur la latence ? | `node test/bancs/banc-transport-binaire.mjs [N=15]` | **accepté et fonctionnel** (trames binaires reçues, zéro delta JSON), mais **aucun gain mesurable** : médiane 835 ms contre 888 ms en JSON, p90 dans l'autre sens, l'écart est sous le bruit. Ne vaut que pour le CPU et la bande passante, pas pour la latence |
| `banc-doublure.mjs` | Le module `lib/doublure.js` seul, sans Twilio ni pont : elle s'ouvre, reçoit un historique en texte, répond, et ne redit pas ce qu'on lui a fait dire. | `node test/bancs/banc-doublure.mjs` | prête en ~1,4 s ; répond en 0,9 s ; après une réponse injectée qu'elle n'a pas produite, elle enchaîne dessus (« Parfait, Julien. Pour quelle heure… ») sans la répéter |

Pour un appel RÉEL enregistré (Twilio `Record=true`, deux voies), la transcription horodatée se fait avec `node C:\Users\msi\.claude\scripts\timeline-appel.mjs <appel.wav>`. La doctrine qui découle de ces mesures est dans le skill `agent-voice`, `references/telephony-reception.md` § 7 bis.

## Fin de tour : remplacer le seuil de silence par un modèle

Chantier `feat/fin-de-tour`. Le pont attend aujourd'hui un silence sec (900 ms chez Palazzo) avant de répondre : c'est le plus gros poste de latence qui reste, et il coupe la parole à qui hésite. Un modèle de fin de tour regarde la forme d'onde et décide beaucoup plus tôt.

⚠ **Ne jamais juger un modèle de fin de tour sur de la SYNTHÈSE.** Une voix de synthèse à qui on donne un fragment le prononce avec une intonation descendante, donc comme une phrase finie : le modèle répond « terminé » à « Mon numéro c'est zéro six » (0,989) et il a acoustiquement raison, le même début coupé dans l'enregistrement de la phrase ENTIÈRE tombant à 0,006. Les fixtures de `fabriquer-pauses.mjs` ne mesurent donc que le seuil de silence (qui, lui, ne regarde que les blancs), jamais un modèle de prosodie.

| Banc | Ce qu'il tranche | Commande | Résultat du 18/09 |
|---|---|---|---|
| `fabriquer-pauses.mjs` | Fabrique 16 fixtures de synthèse (fins de phrase, hésitations, épellations, interjections). | `node test/bancs/fabriquer-pauses.mjs` | 16 WAV 8 kHz + `pauses.json`. Valables pour le seuil de silence UNIQUEMENT |
| `banc-fin-de-tour.mjs` | La courbe de référence du pont : pour un délai donné, combien de coupures et combien d'attente, avec le détecteur d'énergie réel. | `node test/bancs/banc-fin-de-tour.mjs` | la courbe du seuil, celle qu'un modèle doit battre |
| `banc-smart-turn.mjs` | Le portage Node de Smart Turn v3 répond-il comme le modèle d'origine ? | `node test/bancs/banc-smart-turn.mjs` | mel Whisper en Node pur + ONNX int8 : le portage tient |
| `banc-smart-turn-bande.mjs` | Le 8 kHz téléphonique casse-t-il le modèle, entraîné en 16 kHz large bande ? | `node test/bancs/banc-smart-turn-bande.mjs` | **non** : écart moyen de probabilité +0,074 entre large bande et téléphone, sous 0,03 sur 9 cas sur 10 |
| `eot-corpus.py` | Récupère le corpus **réel** : `livekit/eot-bench-data`, part française, CC-BY-4.0. 400 tours humains face à un agent, 71 min, 654 hésitations en cours de phrase et 400 vraies fins de tour étiquetées. Non versionné (~140 Mo). | `python test/bancs/eot-corpus.py` | `fixtures/eot-livekit/` : 400 WAV 16 kHz + `manifeste.json` |
| `banc-eot-scores.mjs` | Interroge Smart Turn CAUSALEMENT à chaque silence du corpus, à 13 délais d'action, avec l'audio qu'il aurait vraiment eu à cet instant. Se shardé (`--part=i/n`) ; `--bande=telephone` fait passer l'audio par la ligne (passe-bas 3,4 kHz, décimation, aller-retour mu-law, remontée à 16 kHz). | `for i in $(seq 0 11); do node test/bancs/banc-eot-scores.mjs --bande=telephone --part=$i/12 & done` | ~8 000 inférences par bande, 4 min sur 12 processus. Coût d'une inférence, seule sur la machine : **médiane 103 ms** (dont **68 ms d'ONNX**, le reste étant le mel en JS pur) |
| `banc-eot-politique.mjs` | L'arbitrage complet, sans refaire une inférence : balaye seuil × délai × filet, sort le front de Pareto, et compare **au détecteur du pont rejoué sur les mêmes tours réels**. | `node test/bancs/banc-eot-politique.mjs` | voir ci-dessous |

**Ce que ça donne, bande téléphonique, sur les 400 tours réels.** Le pont d'aujourd'hui à 900 ms de filet : **17,6 % des hésitations coupées**, 685 ms d'attente moyenne sur une vraie fin de tour. Avec Smart Turn :

| coupures max | pont (filet → attente moyenne) | Smart Turn (attente moyenne, part qui retombe sur le filet) | gain |
|---|---|---|---|
| 2 % | 1500 ms → 1285 ms | 1173 ms (33 %) seuil 0,97 délai 1000 filet 1500 | 112 ms |
| 5 % | 1500 ms → 1285 ms | 876 ms (40 %) seuil 0,98 délai 600 filet 1200 | 410 ms |
| 10 % | 1200 ms → 985 ms | 697 ms (43 %) seuil 0,98 délai 500 filet 900 | 289 ms |
| 18 % | 900 ms → 685 ms | 499 ms (41 %) seuil 0,98 délai 150 filet 800 | 186 ms |

Deux lectures, et la seconde vaut mieux que la première : à latence égale (~690 ms), on passe de **17,6 % à 10 % d'hésitations coupées**, soit 40 % d'interruptions en moins ; à taux de coupure égal, on gagne 190 à 410 ms d'attente MOYENNE (la médiane gagne 200 à 600 ms, mais 30 à 43 % des vraies fins de tour ne sont pas reconnues par le modèle et retombent sur le filet, ce qui mange une partie du gain). La large bande fait mieux d'environ 80 ms d'attente moyenne et de 10 points de retombée sur le filet : **la ligne téléphonique coûte quelque chose, mais peu**, et elle ne remet pas le chantier en cause.
